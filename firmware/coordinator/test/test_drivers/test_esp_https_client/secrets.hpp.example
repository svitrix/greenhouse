#pragma once
// Copy to secrets.hpp and fill in:
//   - WIFI_SSID / WIFI_PASS — your dev Wi-Fi
//   - BACKEND_URL — e.g. "http://192.168.1.42:8000/ingest" (laptop IP +
//     /ingest path)
//   - BACKEND_KEY — output of `python -m app.tools.mint_key gh-hwtest`
//     run against the local backend (Task A9).
// secrets.hpp is in .gitignore; do not commit it.

#define WIFI_SSID    "your-ssid"
#define WIFI_PASS    "your-password"
#define BACKEND_URL  "http://192.168.1.42:8000/ingest"
#define BACKEND_KEY  "paste-the-64-char-hex-key-here"
