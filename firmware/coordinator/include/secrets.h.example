#pragma once
// All credentials are now provisioned via the web UI on first boot
// (or after holding the BOOT button for 3 seconds at startup).
// See docs/superpowers/specs/2026-05-15-wifi-provisioning-design.md.
//
// On first boot the device exposes an open Wi-Fi network named
// "Greenhouse-Setup-XXXX" with a captive portal — connect from any
// phone/laptop and the wizard opens automatically.
//
// This file is kept as a placeholder so existing #include "secrets.h"
// (if any) continues to find a header.
