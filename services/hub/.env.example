DB_URL=postgresql+asyncpg://postgres:dev@localhost:5432/greenhouse
DEVICE_API_KEYS={"gh-a1b2c3d4": "dev-secret-key"}
LOG_LEVEL=INFO

# Telegram bot (separate services/bot/ service) — see services/bot/.env.example.
# The compose `bot` service reads these from the shell / hub .env:
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=
HUB_ADMIN_TOKEN=
